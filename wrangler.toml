name = "faircount-api"
main = "src/workers/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# Development settings
[dev]
port = 8787

# D1 Database
[[d1_databases]]
binding = "DB"
database_name = "faircount-db"
database_id = "local" # Will be replaced with actual ID in production
migrations_dir = "drizzle/migrations"

# R2 Storage (for avatars, exports) - uncomment when ready
# [[r2_buckets]]
# binding = "STORAGE"
# bucket_name = "faircount-storage"

# Environment variables
# Note: These are LOCAL values. Production values are injected by GitHub Actions.
[vars]
APP_URL = "http://localhost:3000"
APP_NAME = "FairCount"
